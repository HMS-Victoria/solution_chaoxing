# 验证记录（2026-09-20）

状态：**包已生成，实际环境验收待完成**。已完成本地源码、固定构建、非交互式包检查与部分真实桌面验收。未安装 Python 的新 Windows 用户验收未完成。

## 环境和证据

Windows x64，内核 10.0.26200；开发机存在 Python。打包使用 CPython 3.9.13 x64、PyInstaller 6.20.0、hooks 2026.4，其余依赖见 `build-record.json`。源码提交 `8365e18ffbe0a9fa6f57780889a00a205398abfd`。

初始沙箱用户无法读取安装目录中的 Tcl 文件，误表现为 Tcl 缺失；在有读取权限的宿主检查 Tcl 8.6.12 成功。之后沙箱账号启动发生 `CreateProcessWithLogonW failed: 1909`，本任务项目内构建通过已审批的宿主执行完成。没有修改 Python 安装、系统 PATH 或安全设置。此过程不能用作普通权限新用户验收证据。

## 已执行检查

| 检查 | 结果与边界 |
|---|---|
| 基线/远端刷新 | 本地初始干净，远端 main 为 ad371f8；开工 Release 为空；凭据助手认证仅在内存使用，确认该仓库可推送 |
| 源码 `--selftest` | 通过：INPUT 结构体尺寸、中文/emoji UTF-16、换行标准化与按键序列 |
| `test_release.py` | 通过：中文/换行/emoji 事件、关闭换行跳过、零/部分注入失败、自身焦点取消、预先停止、并发渲染不会吞掉完成/停止消息；全部模拟，未发送真实按键 |
| 固定构建 | 最终两次独立工作目录/缓存构建成功；来源与完整日志见 `build-record.json`、`BUILD_LOG.txt` |
| 重复构建 | 最终 exe 两次 SHA-256 完全一致，见 `reproducibility.json`；仅证明本机同一固定环境，不推广为任意机器字节一致 |
| 防覆盖 | 已有 dist/v1.0.0 时返回非零并提示 Output already exists，未覆盖原包 |
| PE/包内容 | AMD64、GUI subsystem、版本 1.0.0.0、asInvoker；包内含 python39.dll、_tkinter.pyd、Tcl/Tk DLL 和脚本，共 990 项 |
| 冻结 exe 自检 | 在独立临时目录、独立 profile 环境变量、PATH 仅 Windows System32 下 `exe --selftest` 返回 0；仍是原开发机与账号，不是干净新用户 |
| 文件校验 | exe 与中文说明符合 SHA256SUMS；程序 9 MB 级，精确大小见发行资产和发布记录 |
| 敏感文件检查 | PyInstaller 仅从单一源码入口收集依赖；归档成员没有 .pem/.key/.env/.log，发行目录仅三个白名单文件，无用户文本或登录态 |
| Git 差异检查 | `git diff --check` 通过；历史 exe 未改动 |

PyInstaller 警告集中于 Windows 不使用的 posix/pwd/grp/resource 等条件模块及未调用的 pep517 路径；Tk/Python 运行库经独立结构检查确认存在。首次构建版本资源括号错误已修复；失败日志保留在 build/v1.0.0-20260920-201622/。首次检查脚本未统一归档路径分隔符而误报 init.tcl 缺失，修正后通过，未用该误报修改打包内容。

## 真实桌面与限制

通过 computer-use 的受支持 Windows 接口执行，只操作本次测试程序与自造文本；未输入真实作业系统。

1. 候选 exe（修复状态竞争之前，SHA-256 `c07fd49c9c4dea194bbedb949efe0b13f13572036ecb5b55802f5aa0b83aadf7`）实际启动两个独立进程。
2. 源窗口手工输入 `中文验收 😀`，按回车，再输入 `第二行 ABC123`。设置 10 秒倒计时、点击开始、切到另一个进程的空白文本框，实际收到两行中文/emoji/ASCII；源状态“完成：已输入 17 个字符”。证据：`desktop-target.png`、`desktop-completed.png`。
3. 目标始终为前台窗口。候选开始后停留源窗口时观察到焦点取消；未完整实测输入中途切到第三个窗口的后续去向。
4. 候选停止试验出现倒计时文字滞留，截图 `desktop-stop-before-fix.png` 是缺陷证据，不能当作通过截图。定位并修复 _pump/_post 消息覆盖竞争；回归测试模拟渲染过程中到达的完成消息，验证它得以保留。
5. **最终 exe**（SHA-256 `6156ab89153aac8ff3c3649d23f0c816e8746fa1222b38e832e253e8a15ab933`）重新真实启动，输入“停止验证”，为覆盖工具操作延迟在倒计时框手动填 30，点击开始再点击停止，观察“已取消”且恢复可开始。证据：`desktop-final-stop.png`。所有本次测试创建的工具实例关闭，不保存用户数据。
6. 记事本启动先出现授权超时、再出现窗口发现延迟。刷新后发现的是用户已有文档窗口；尝试新建空白页前的应用授权又超时，未获得空白记事本测试证据，未编辑已有文档。
7. 自动化短按 F8 未观察到触发，快捷键列为未通过验收/待手动核实；按钮开始路径已经实际运行。F8 原有 80 ms 轮询方式保留。

## 仍需完成的真实验收

使用最终发行下载文件，在**未安装 Python 的新 Windows 用户/干净虚拟机、普通权限**中：

- 双击启动（不通过源码脚本），确认不需 Python/联网/账号。
- 空白记事本中输入上述中文、回车、emoji，核对原文。
- 验证 F8 开始、按钮倒计时开始、倒计时取消、长文本输入中停止以及关闭退出。
- 仅在两个自造空白编辑窗口之间切换，核对输入确实跟随当前前台窗口；不要使用聊天、命令行或真实业务窗口作目标。
- Windows 10 x64 的兼容性、首次下载来源提示与管理员目标拒绝路径均未实测。

源码/包自检、候选版桌面测试、最终版部分桌面测试均不能替代以上完整验收。公开下载验证见 `publication.json`；下载哈希一致也不等于交互验收完成。

## 复核入口

```cmd
.venv-build\Scripts\python.exe type_at_cursor.pyw --selftest
.venv-build\Scripts\python.exe test_release.py
.venv-build\Scripts\python.exe docs\release-readiness\2026-09-20\verify_package.py
```

不要直接重建覆盖已有发行目录。先明确归档，再运行 build_exe.cmd；构建和校验记录应与待发布源码保持一致。

## 发布过程补充

GitHub 将上传的中文文件名规范化为 `-Windows-x64.exe` / `default.txt`，即使修改资产 name 字段也一样。已统一采用 `TypeAtCursor-Windows-x64.exe` 与 `README-zh-CN.txt`，中文界面和说明正文保留。首次公开下载使用了草稿阶段的 untagged 链接而返回 404；最终下载以重新查询的公开 v1.0.0 资产链接为准。

本次命名修正提交为 `8365e18ffbe0a9fa6f57780889a00a205398abfd`，最终构建目录为 `build/v1.0.0-20260920-211939/`。重新构建后 exe SHA-256 与已验收的 6156ab89... 完全一致，因此最终停止截图和原有最终包自检仍对应相同二进制。中文说明及 SHA256SUMS 已同步更新。

已发布的 v1.0.0 标签仍指向首次留档提交 d2dd09d，不重写已公开标签；运行时源码与当前打包提交完全相同。标签源码中的旧发行文件名不再用于上传，复现正式资产应使用上述 8365e18 提交或当前 main。Release 正文提供固定源码链接。实际发布资产和公开下载核对结果见 publication.json。

## 公开发布后复核结果

2026-09-20 21:22 +08:00，v1.0.0 的 exe、中文说明、SHA256SUMS 三个正式公开链接均在不附认证头的情况下下载成功，三个文件全部与本地哈希一致；重新下载的 exe 在无 Python PATH 下自检退出码为 0。详见 publication.json。记事本与新 Windows 用户等上述待办不因此变为已通过。
